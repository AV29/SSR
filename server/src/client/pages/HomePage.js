import React from 'react';

const HomePage = () => {
    return (
        <h1 style={{ textAlign: 'center' }}>I'm the super-duper home component</h1>
    );
};

export default {
    component: HomePage
};
